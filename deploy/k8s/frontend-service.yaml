apiVersion: v1
kind: Service
metadata:
  name: agents-frontend
  labels:
    app.kubernetes.io/name: agents-frontend
spec:
  selector:
    app.kubernetes.io/name: agents-frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
