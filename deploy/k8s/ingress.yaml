apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agents-frontend
  annotations:
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agents-frontend
                port:
                  name: http
