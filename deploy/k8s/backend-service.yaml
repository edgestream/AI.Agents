apiVersion: v1
kind: Service
metadata:
  name: agents-backend
  labels:
    app.kubernetes.io/name: agents-backend
spec:
  selector:
    app.kubernetes.io/name: agents-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
