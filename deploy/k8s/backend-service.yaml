apiVersion: v1
kind: Service
metadata:
  name: ai-agents-backend
  labels:
    app.kubernetes.io/name: ai-agents-backend
spec:
  selector:
    app.kubernetes.io/name: ai-agents-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
