apiVersion: apps/v1
kind: Deployment
metadata:
  name: agents-news
  labels:
    app: agents-news
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agents-news
  template:
    metadata:
      labels:
        app: agents-news
    spec:
      containers:
        - name: agents-news
          image: ghcr.io/edgestream/agents-samples:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8888
          env:
            - name: ASPNETCORE_URLS
              value: http://+:8888
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
